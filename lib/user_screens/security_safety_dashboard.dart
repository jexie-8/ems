import 'package:flutter/material.dart';

class SecuritySafetyDashboard extends StatelessWidget {
  const SecuritySafetyDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Security_Safety Dashboard')),
      body: const Center(child: Text('Welcome Security_Safety!')),
    );
  }
}
